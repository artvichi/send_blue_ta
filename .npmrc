save-exact=true

# npm 10's peer-dependency resolver crashes on this graph (an arborist
# "edgesOut of null" bug, triggered by Prisma's self-referential peer set).
# This is the documented workaround and matches the resolution pnpm was using
# before the switch; it does not relax any version we actually depend on.
legacy-peer-deps=true
